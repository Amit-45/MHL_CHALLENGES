import os
import datetime
import time
n=1
c,b,a=input("Enter Date: ").split("/")
hr,minn, sec=input("Enter the Time = ").split(":")
shedule_date = datetime.date(int(a),int(b), int(c)
n=1
while n>0:
    if time.localtime().tm_hour= int(hr) and
        os.startfile("C:\\Users\\AmitPatnaik\\beep.mp3) //file in my local pc
        break
     else:
        n+=1
